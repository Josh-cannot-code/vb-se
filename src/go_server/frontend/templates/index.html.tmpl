<!DOCTYPE html>
<html>

<head>
    <script>{{ . }}</script>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        :root {
            --light-green: #3B8C6E;
            --dark-green: #27592F;
            --soft-orange: #F2A25C;
            --soft-brown: #A6653F;
            --soft-red: #A63232;
        }

        #page-title {
            text-align: center;
            padding: 2rem;
            margin: 0;
            margin-bottom: 0.5rem;
            color: var(--dark-green);
            border: none;
            font-weight: lighter;
            font-size: 40pt;
        }
        h2 {
            text-align: center;
            padding: 2rem;
            margin: 0;
            margin-bottom: 0.5rem;
            color: var(--dark-green);
            border: none;
            font-weight: lighter;
            font-size: 30pt;
            grid-column: 2;
        }

        body {
            margin: 0;
            padding: 0;
            background: #333333;
            font-family: Arial, sans-serif;
            font-weight: light;
        }

        .search {
            flex-grow: 1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .search input[type="text"] {
            padding: 0.3rem;
            border: none;
            border-radius: 0.2rem;
            background: var(--dark-green);
            font-size: 14pt;
            width: 20rem;
            margin: 0.5rem;
        }

        .search button[type="submit"] {
            padding: 0.3rem;
            background: var(--dark-green);
            border: none;
            border-radius: 0.2rem;
            font-size: 14pt;
        }

        .video-container {
            display: grid;
            grid-template-columns: auto min(90%, 30rem) auto;
        }

        .video-card {
            grid-column: 2;
            margin-top: 1rem;
            padding: 1rem;
            border-radius: 0.7rem;
            background: var(--dark-green);
        }

        .thumbnail {
            border-radius: 0.7rem;
            width: 100%;
        }

        .video-title {}

        .video-description {
            overflow: hidden;
            text-overflow: ellipsis;
            max-height: 8rem;
        }
    </style>
</head>

<body>
    <h1 id="page-title">
        Vlogbrothers Search Engine
    </h1>
    <div class="search">
        <form>
            <input type="text" placeholder="Search Vlogbrothers Youtube Channel">
            <button type="submit" hx-get="/videos?search=em-dash" hx-swap="outerHTML" hx-target="#video-container">
                Search
            </button>
        </form>
    </div>
    <div class="video-container" id="video-container">
        <h2>
            Search for something like "em dash"
        </h2>
    </div>
</body>
</html>
